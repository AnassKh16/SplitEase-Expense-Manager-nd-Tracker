# Supabase project URL — found in Supabase Dashboard → Settings → API
VITE_SUPABASE_URL="https://zamdxmzppknkcvtslket.supabase.co"

# Supabase anon/public key — found in Supabase Dashboard → Settings → API
VITE_SUPABASE_ANON_KEY="sb_publishable_DjtIs7WghSer0woNkj5QPw_DcG2cTLA"

# The URL where this app is hosted (used for OAuth redirects)
# Local development: http://localhost:3000
# Production: https://yourdomain.com
VITE_APP_URL="http://localhost:3000"

# Optional: Supabase Storage bucket id (default: splitease). Must match Dashboard → Storage.
# VITE_SUPABASE_STORAGE_BUCKET="splitease"